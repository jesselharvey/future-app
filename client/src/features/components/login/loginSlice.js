import React from 'react'
import { createSlice } from '@reduxjs/toolkit'

export const loginSlice = createSlice({
  name: 'login',
  state: {
    username: '',
    password: '',
  },
  reducers: {
    
  }
})